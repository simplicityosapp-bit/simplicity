# Monorepo root — per-app .gitignore files live under apps/*/

# dependencies (hoisted to root by pnpm's node-linker=hoisted)
node_modules/
.pnpm-store/

# build output
dist/
build/
web-build/

# Expo / Metro
.expo/
.metro-health-check*

# TypeScript
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# local credential / test artifacts (were covered by web's .gitignore pre-move)
.claude-*.json
**/.claude/worktrees/

# supabase CLI temp
supabase/.temp/

# OS
.DS_Store
Thumbs.db
