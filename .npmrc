# Expo/Metro needs a flat, hoisted node_modules layout (no nested symlink tree).
# This keeps pnpm's speed + single lockfile while behaving like npm on disk.
node-linker=hoisted

# React Native / Expo peer graphs are noisy; don't fail installs on peer mismatches.
strict-peer-dependencies=false
